import 'package:flutter/material.dart';

class finalBage extends StatelessWidget {
  const finalBage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text("no link"));
  }
}
